<!-- Reference: references/android-platform.md and references/proximity-presentation.md -->
<!-- Multipaz NFC credential presentation is currently Android-only. Do not reuse this for iOS. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-permission android:name="android.permission.NFC" />

    <application>
        <service
            android:name=".YourCombinedNfcService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/combined_nfc_service" />
        </service>
    </application>
</manifest>
